version: '3'
services:
  opensearch:
    image: opensearchproject/opensearch:2.11.0  # 버전 명시 권장
    container_name: opensearch-node
    environment:
      - discovery.type=single-node
      - "OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m"
      # 👇 POC용 핵심 수정: 보안 비활성화 (비번/SSL 없이 HTTP 접속 허용)
      - "plugins.security.disabled=true"
    ports:
      - "9200:9200"
      - "9600:9600"
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
      memlock:
        soft: -1
        hard: -1